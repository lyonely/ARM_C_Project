#ifndef MULTIPLY_H
#define MULTIPLY_H
#include <stdio.h>
#include "datatypes.h"
#include "functions.h"

/* Executes the multiply instruction */
void multiply(Instruction instruction, struct Registers* registers);

#endif

