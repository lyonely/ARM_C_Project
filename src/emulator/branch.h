#ifndef BRANCH_H
#define BRANCH_H
#include <stdio.h>
#include "datatypes.h"
#include "functions.h"

/* Executes the branch instruction. */
void branch(Instruction instruction, struct Registers* registers);

#endif

